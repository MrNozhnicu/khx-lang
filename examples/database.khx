// Работа с базой данных

print("=== База данных KHX ===")
print("")

let db = connect_database("sqlite", "users.db")
print("База данных подключена")

db_execute(db, "CREATE TABLE IF NOT EXISTS users (id INTEGER, name TEXT)")
print("Таблица создана")

db_execute(db, "INSERT INTO users VALUES (1, 'Иван')")
db_execute(db, "INSERT INTO users VALUES (2, 'Мария')")
db_execute(db, "INSERT INTO users VALUES (3, 'Петр')")
print("Данные добавлены")

print("")
print("✓ База данных готова!")
